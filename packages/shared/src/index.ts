/**
 * `@npm-manager/shared` — types and pure helpers with no dependency on Node,
 * VS Code, Visual Studio, or any host. Safe to import from the engine, either
 * extension host, and the webview UI alike.
 */

export * from "./messaging";
export * from "./versionRange";
export * from "./semverUtil";
